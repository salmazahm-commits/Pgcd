# nombres premiers 
#include <stdio.h>
int main () {
    int n,i;
    if (n < 2)
        return 0;

    for (i = 2; i <= n / 2; i++)
    {
        if (n % i == 0)
            return 0;
    }
    return 1;
}

int main()
{
    int i, A;

    /* 1. Affichage des nombres premiers de 1 à 100 */
    printf("Les nombres premiers de 1 a 100 sont :\n");
    for (i = 1; i <= 100; i++)
    {
        if (estPremier(i))
            printf("%d ", i);
    }

    /* 2. Saisie d’un nombre A */
    printf("\n\nEntrez un nombre A : ");
    scanf("%d", &A);

    /* 3. Test si A est premier */
    if (estPremier(A))
        printf("%d est un nombre premier.\n", A);
    else
        printf("%d n'est pas un nombre premier.\n", A);

    return 0;
}
